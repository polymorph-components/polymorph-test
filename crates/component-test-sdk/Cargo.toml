[package]
name = "component-test-sdk"
description = "Guest SDK for polymorph:test suites"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
component-test-core = { workspace = true }
component-test-sdk-macro = { workspace = true }
foldhash = "0.2.0"
indexmap = "2.14.0"
wit-bindgen = { workspace = true }

[dev-dependencies]
# Pins the `or_fail()` contract against the motivating error type.
anyhow = { workspace = true }
# UI tests for the #[suite] macro's compile-time guards (the compile
# errors are the product; see tests/ui.rs).
trybuild = { workspace = true }
