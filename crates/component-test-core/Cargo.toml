[package]
name = "component-test-core"
description = "Core data model for lann:component-test: case-name grammar, feature marks, verdicts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true, optional = true }

[features]
default = ["serde"]
serde = ["dep:serde"]
