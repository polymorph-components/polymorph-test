[package]
name = "component-test-core"
description = "Core data model for polymorph:component-test: case-name grammar, feature marks, verdicts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
arcstr = { version = "1.2.0", features = ["serde", "substr"] }
serde = { workspace = true, optional = true }

[features]
default = ["serde"]
serde = ["dep:serde"]
