[package]
name = "runner-cli"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true, features = ["futures-stream"] }
futures = { workspace = true }
# The frozen results schema (#26): linked, never hand-rolled — schema
# evolution must not have per-runner encoders to keep in sync (#34).
component-test-results = { workspace = true }
serde_json = { workspace = true }
